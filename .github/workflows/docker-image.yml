name: Docker Image CI

on:
  push:
    branches: [ master ]

jobs:

  build:
    container:
      image: revisionsystems/ci-cd:latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: echo test
